{
  "name": "trustworthy-memory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --test \"tests/**/*.test.ts\"",
    "bench": "node bench.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=24" },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
